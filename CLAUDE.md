# 給 AI 的專案須知

## ⚠️ 動選股邏輯前，先讀 [BACKTEST_FINDINGS.md](BACKTEST_FINDINGS.md)

本專案的選股模型**已做過 139 期滾動回測驗證**，結論已寫入
[`BACKTEST_FINDINGS.md`](BACKTEST_FINDINGS.md)（原始數據在 `backtest_results.json`）。
以下方向**已被資料否定，請勿重複提出**：

### ⚡ 最重要：策略有效性**取決於大盤環境**，不可一概而論

5年179期分環境檢驗（1個月持有超額報酬）：

| 模型 | 多頭(124期) | 震盪(28期) | 空頭(27期) |
|------|-----------|-----------|-----------|
| 長線+量能確認 | **+3.95%** | +0.55% | +0.62% |
| 純動能60日 | **+4.68%** | -0.25% | **+2.06%** |
| 潛力潛伏 | -0.98% | -0.45% | **+2.23%** |
| 攻守兼備 | -1.26% | -0.22% | **+1.13%** |

**動能類在所有環境皆為正（最穩健）；低基期/潛力類是逆勢型——多頭失效、空頭最強。**
**不要再寫死「潛力策略無效」的結論。**

### 🧪 但「依盤勢切換策略」已被走查驗證否定 —— 不要做大盤濾網

前89期挑模型、後90期驗證（完全 out-of-sample）：

| 策略 | 超額報酬 | 勝率 |
|------|---------|------|
| 單押 長線 | **+5.40%** | 73.3% |
| 單押 長線+量能確認 | +5.27% | **77.8%** |
| **大盤濾網切換** | **+3.05%** | 54.4% ⬅ 更差 |

切換總是慢半拍（確認空頭時跌勢已走完一段）。而且**模型挑選本身會過擬合**：
訓練段最佳的「純動能60日」在測試段只有 +3.16%，輸給沒被選中的長線。

**任何新想法都要用走查驗證（train/test 分段），不能用全期資料挑模型再用全期驗證。**

### 📐 持股檔數：10~20 檔最佳
5 檔的超額報酬與 10 檔相同（+3.46%）但 t 值更低、勝率更差——**集中到 5 檔只增加波動、
換不到報酬**。要報酬用 10 檔、要穩定用 20 檔（t=6.10、贏基準率 66.5%），30 檔開始稀釋。

### ⛔ 估值面（本益比等）至今無法驗證 —— 別假裝驗證過了
證交所 `BWIBBU_d` 有歷史估值資料，但**限流極嚴**：加了指數退避後 60 個日期仍有 43 個被擋。
**被擋時回傳非 JSON**，若把例外默默吞掉，會讓「抓不到資料」偽裝成「因子無效」，
產出看似合理實則無根據的結論。`get_hist_valuation()` 已加失敗計數與報表警示——
**看到警示就不要引用估值類模型的數字**。跑估值需加 `--valuation` 旗標（預設關閉，因為很慢）。

| 其他已被否定的方向 | 實證結果 |
|------|---------|
| 用 ATR 風報比 R:R 排序選股 | 全期間超額為負（但空頭 +1.18%，同屬逆勢型） |
| 機械式 2×ATR 停損 | 3個月超額由 +12.54% 崩到 **+4.21%**，持有越久傷害越大 |
| 用「低波動」過濾長線分 | +3.10% → **+0.31%**（t=0.90 不顯著），波動正是報酬來源 |
| 用「排除接近52週高點」過濾 | +3.10% → +2.05%，不追高反而有害 |

**目前最佳模型：「長線結構分 + 量價未轉弱」**
（1個月超額 **+3.36%**、t=5.59、贏基準率 64%；3個月超額 **+12.60%**、t=6.95）。
已設為 App 預設策略 `bestproven`。

**另一項驗證**：本專案的複合長線分**優於所有單純動能因子**
（+3.10% vs 純動能60日 +2.81% / 250日 +2.16% / 120日 +2.05% / 學術12-1 +1.79%），
代表複合設計有實質加值。

若要主張某個新選股邏輯有效，**請先用 `backtest_research.py` 驗證**，不要只靠直覺：
```bash
python3 backtest_research.py --stocks 450 --every 5 --years 3 --topn 10
```
判讀重點：**超額報酬**（贏過「隨便買」多少）才是選股能力的證據，絕對報酬在多頭時人人都高；
**t值 ≥1.96** 才代表不是運氣。

## 核心架構原則

- **`services/analysis.py` 是唯一計分核心**。個股分析與智能選股都必須呼叫
  `prepare_frame()` + `compute_scores()`。兩邊曾各自實作導致同一檔股票分數不同
  （台積電綜合 69 vs 74），已統一，**請勿再各寫一套**。
- 指標一律以 `SCORING_PERIOD`（2年）歷史計算，確保 MA120／Vol_MA60／52週區間有效。
  使用者選的「分析期間」**只切圖表，不影響評分**。
- 全市場掃描走 `services/universe.py` 的批次路徑（yfinance 批次下載 + 證交所批次 API），
  逐檔查詢會慢上百倍。

## 已知資料陷阱

- **yfinance `dividendYield` 是百分比數字**（3.84 = 3.84%），不是分數。
  已在 `fundamental._normalize_dividend_yield()` 統一正規化，別再乘 100。
- **yfinance 對台股回傳英文名**（`shortName` = "POU CHEN"）。
  顯示名稱一律用 `stock_lookup.resolve_company_name()`（中文優先）。
- **基本面評分曾嚴重飽和**（7 檔並列 100 分），已依台股實際分位數重新校準並加 tanh 平滑，
  修改門檻前請先確認分數分布沒有再度擠在頂端。
- 回測**無法還原**基本面與新聞（無歷史快照），納入會造成前視偏誤。

## 慣例

- 使用者以繁體中文溝通，UI 文案與文件用繁中；**git commit message 用英文**。
- 每次改完用 Streamlit `AppTest` 驗證頁面不拋例外，再重啟服務。

### 🧱 不要疊加更多過濾條件；也不要跨 run 比較模型
- 「長線+量能確認」單一過濾即最佳。加中線/融資/四重確認**都更差**（四重 2.11% < 單一 2.57%）
- **跨 run 名次會變**：同模型三次執行全距達 0.6~0.76%，大於模型間差異。
  yfinance 每次下載成功的股票不同 → universe 不同。**只在同一次執行內比較。**
- 「長線+融資減少」曾在某次跑出 +3.28% 看似最佳，實為雜訊（全距 0.76%）。已維持原預設。

### 📄 上櫃已納入（2026-09-08）
`services/universe.py` 新增櫃買中心 API（`get_otc_snapshot`），全市場 = 上市1087 + 上櫃887。
yfinance 後綴：上市 `.TW`、上櫃 `.TWO`，由 `stock_data._is_otc()` 依官方清單判斷。
⚠️ 舊 `OTC_STOCKS` 曾把大立光/緯穎/緯創誤標為上櫃（它們是上市），已修正。
融資資料目前仍僅上市有，上櫃股不計融資扣分。

### 🏭 族群動能有效，但「補漲」無效（2026-09-08）
以官方產業別分族群、算 60 日報酬中位數排名：
- **「前5強族群 + 長線分最高」= 目前最佳模型**（+3.07%, t=5.32，兩次執行一致），
  已設為 App 預設策略 `sectorhot`（優於 `bestproven` 的 +2.58%）
- **「強勢族群裡的落後股（補漲）」超額 −0.25%、贏基準率僅 39.7% —— 不要做補漲**
- 動能在族群與個股兩個層級都有效；均值回歸兩個層級都無效

## 🧩 共用元件：改一次，三頁同步（2026-09-08）
計分早已統一在 `services/analysis.py`；**顯示**也已統一在 `services/ui.py`。
以前同樣的東西寫了 3 遍（週期分數格、本益比徽章、實證區間徽章），
每次調整要三邊各改一次，很容易漏掉造成同數字在不同頁長相不一。

**新增／修改卡片元素時，一律改 `services/ui.py`，不要在 app.py 內嵌 HTML：**
- `horizon_cells(horizon, selected_key=...)` — 四格週期分數 + 效力標記
- `pe_badge(pe, dy, highlight=)` / `pe_inline(...)` — 本益比徽章
- `evidence_badge(long_score)` — 長線分實證區間（勝率／超額）
- `rr_cell(rr, stop_pct, atr_pct)` — 風報比
- `long_threshold()` — 三頁共用的買進門檻（勿再硬寫 70）

這些函式**只回傳 HTML 字串**、不呼叫 st.*，方便嵌進各頁不同容器。

## 📋 新增選股策略：只改 `services/strategies.py` 一個地方
策略原本散在 app.py 的 12 處（選項、說明、if-elif 篩選排序、初篩鍵、圖表指標、
顏色、實證對照、是否吃週期選單…），漏一處就出事——已發生過兩次：
`bestproven` 漏初篩鍵→KeyError 整頁掛；週期選單對 6/7 策略其實無作用卻看不出來。

現在改為**宣告式單一表**：在 `STRATEGIES` 加一筆 dict 即可，必填欄位由
`validate()` 在 import 時檢查（漏填/重複 key 會直接拋錯，而非等使用者點到）。
必填：key/label/caption/bar_note/sort_desc/prelim_key/color/uses_horizon/
evidence_model/metric/select。

`select(results, ctx)` 負責篩選＋排序，ctx 提供 buy_bar 與 horizon_key。
**不要再在 app.py 寫 `if strategy == ...` 的篩選邏輯。**

## 💾 快取策略：換策略絕不重抓（2026-09-08）
1. **`download_history_bulk` 有 `@st.cache_data(persist="disk")`** —— 全市場歷史下載是
   最貴的一步，原本完全沒快取。實測 291 檔：冷啟 4.58s → 命中 0.01s（530x）。
   `persist="disk"` 讓 Streamlit 重啟後仍在。內層 `_download_history_cached` 的參數
   必須可雜湊（tuple/frozenset）；有 progress_cb 時會略過快取（回呼無法雜湊）。
2. **掃描一律用 `SCAN_FLOOR_TURNOVER`（最寬門檻）下載，實際門檻在記憶體過濾** ——
   否則每動一次流動性滑桿就重抓全市場，但高門檻本來就是低門檻的子集合。
3. **session 快取鍵只含「掃描範圍」**（`smart_full` / `smart_pop` / `smart_poplu`），
   **不含策略、週期、流動性門檻**。換策略＝對同一批結果重新排序而已。
4. **全市場模式下漲停股不另掃**——那些股票本來就在掃描範圍內，只需事後標記
   `is_limit_up`，不必因為切到「漲停動能」就換快取鍵重掃。

⚠️ 加新的 UI 控制項時，先想清楚它該不該進 cache_key：只有**改變「掃描哪些股票」**
的才該進去；只影響**排序或呈現**的一律不要，否則使用者每點一下就重抓幾百 MB。

## ⚠️ 兩種「長線分」不可混用（2026-09-09，重要）
- **純技術長線分** = `calculate_horizon_scores(df)["long"]["score"]`
  → **回測與分數門檻分析用的就是這個**。所有實證結論（+3.07%、t=5.32、70分門檻、
    勝率58.7%）都是在它身上量出來的。**排序與實證對照一律用它**（`horizon_tech`）。
- **混合長線分** = `generate_timeframe_recommendations(...)` 的 long
  → 再混入 基本面45% + **目標價30%** + 新聞5%。這三項**沒有歷史快照、無法回測**。

實測差距很大（台積電：純技術 **94** vs 混合 **81**）。曾經 App 用混合分排序卻套用
純技術分的實證門檻，等於把未驗證的成分偷渡進「有實證支持」的策略——已修正。

掃描結果現在同時帶 `horizon_tech`（純技術，供排序/實證）與 `horizon`（混合，供顯示建議）。
`services/strategies._long()` 會優先取 `horizon_tech`。
**新增任何「宣稱有實證支持」的排序時，務必確認用的是回測時同一個指標。**

### 🔢 「長線分」全站只有一個數字（2026-09-09）
四週期卡片／格子**一律顯示純技術分**（`horizon_cards`，由 `horizon_tech` 產生，
建議動作也由同一分數推導）。先前卡片顯示混合分、儀表板顯示純技術分，同一檔股票
出現兩個都叫「長線分」的數字（台虹 88 vs 60），使用者無從判斷該信哪個。
`generate_timeframe_recommendations()` 保留給回測相容，**不再用於顯示**。

App 現在只有兩個對外分數，角色不同：
- **長線結構分**（純技術）= 進場時機訊號，有回測背書（t=4.71）
- **綜合評分** = 體質與風險總覽（含基本面/消息/目標價/融資），預測力較弱（t=2.04）
兩者差距 ≥15 分時，個股頁會自動解釋為何不同，不讓使用者自己猜。

### 🔍 篩選條件即說明（2026-09-09）
`STRATEGIES[*]["filters"]` 是 `[(說明, fn(r,ctx)->(bool, 細節字串))]` 的清單，
`select()` 與 `explain()` **共用同一份**，所以「為什麼沒選到」的解釋不可能與實際結果不一致。
新增條件時只要加進 filters，選股與說明會同步生效——**不要另外寫一套解釋文字**。

需要全體資料才能算的條件（如族群排名）走 `prepare_ctx()` 預先算好放進 ctx。

持股頁每檔都有「🔍 在各選股策略中的入選情形」，逐條顯示 ✅/❌ 與實際數值
（例：萬海長線分 94 ✅ 達買進線，但 ❌ 量價轉弱 −5，故未入選「長線+量能確認」）。

### 🎯 長線分平手必須破（2026-09-09）
長線分只有 **18 種離散值**，156/566 檔並列滿分 **94**——只用它排序＝同分群裡亂數挑。
排序鍵已改為 `_long_key(r) = (長線分, 距季線幅度%)`。回測（兩次一致）：
不破平手 1個月 +0.52%(不顯著)／3個月 **−1.08%**；距季線破平手 +2.51%／**+5.05%**。
**新增任何用長線分排序的地方，一律用 `_long_key`，不要直接用 `_long`。**
先前引用的超額數字帶有平手隨機性，重新量測時請以有破平手的版本為準。

### 📰 新聞評分有反身性，不要當買進理由
股價漲越兇 → 正面報導越多 → 消息分越高。台虹：消息 90 分，但本益比 93、近60日 +104%。
消息面在**排序訊號（長線結構分）中權重為 0%**，只出現在綜合評分的 15%。
判斷在 `technical.overheat_flag()`，示警＝**漲多(近60日>50%)** 且 **貴或熱(本益比>40 或 消息≥75)**。
單純漲多不示警——回測結論是動能有效，對「漲太多」示警會與實證矛盾。

### 🧯 教訓：加共用邏輯時，兩條計分路徑都要接上（2026-09-09）
本專案有**兩條**計分路徑，很容易只改到一條：
1. `analysis.compute_scores()` —— 個股分析頁、我的持股頁、深度分析
2. `universe.scan_universe()` —— 全市場批次掃描（智能選股）

實際踩過的坑：`above_ma120`（平手鍵）只加在掃描路徑，`compute_scores` 沒回傳，
於是**持股頁的平手鍵永遠是 None**，剛做完的排序修正在那一頁等於沒生效；
「利多已反映」警語則相反，只加在 `compute_scores` 且只有個股頁會顯示，
選股頁與持股頁完全看不到。

現在共用判斷一律放 `services/technical.py`，兩條路徑各自呼叫同一個函式：
- `dist_from_ma120(df)` —— 距季線幅度（長線分平手鍵）
- `overheat_flag(pe, r60, news_score)` —— 過熱／利多已反映，回傳 dict
顯示則走 `services/ui.py: overheat_badge()`，三頁同一份 HTML。

**新增任何「每檔股票都該有」的欄位時，檢查清單：**
`compute_scores()` 回傳 → `_analyze_one_stock()` row → `scan_universe()` row → 三頁顯示。

### 🔺 已取消「漲幅已大」扣分（2026-09-09，改過計分公式）
長線分的半年報酬項原本是 `>60% 給 +4`（比 10~60% 的 +10 少 6 分），
實測**方向是反的**：同結構但半年漲 >60% 的那群（原 88 分）3個月超額 +6.43%，
漲 10~60% 的那群（94 分）只有 +2.07%，配對 t=-3.33。改成 +10 後選前10名的
3個月超額 +3.77% → +6.70%（t=2.55），**走查前後半段皆為正**。

⚠️ 副作用：滿分 94 的股票從 156 檔變成 **277 檔（49%）**，
**分數在頂端已幾乎沒有鑑別力，排序實際上全靠 `above_ma120` 這個連續值**。
要再改排序邏輯時，記得真正在做事的是平手鍵，不是分數本身。
漲多的風險改由 `overheat_flag()` 警語處理，不要再用扣分——
扣分會讓報酬最高的一群排不進前段。

`score_thresholds.json` 已用新公式重跑，**70 分門檻仍成立**。

### 🧮 分數是離散的：長線分只有 40 種可能值
= 50 ±14(季線) ±12(MA60vsMA120) ±8(斜率) +10/−10(半年報酬)。
89~93 沒有任何組合到得了，所以會看到「一堆 94 然後直接跳到 84」。
單調性 Spearman ρ=+0.69(1個月)/+0.67(3個月)，大方向正確；
但**分數高不等於勝率高**：94 的勝率最高(70.5%)，報酬卻是 88 那群最高。

## 🎯 排序主訊號已改為「連續趨勢結構分」（2026-09-09，重大變更）
`services/scoring.py` 是新的排序核心：距季線幅度／MA60vsMA120／季線斜率
三個因子各轉成**當日橫斷面百分位**再平均，分數本身就是「贏過全市場幾 %」。

- 全市場 566 檔有 **418 種不同分數**、最大並列 5 檔（舊離散長線分：277 檔並列 94）
- 139 期回測 +3個月超額 **+7.18%、t=5.52、勝率 66%**，是測過 22 個模型裡 t 值最高的
- **加動能／量能／資券／估值全部更差**（見 BACKTEST_FINDINGS 第十三輪表）
- 買進線 = **50**（`app.TREND_BUY_BAR`）。實測 40 以下超額穩定為負、50 以上轉正；
  優勢集中在 **90 分以上**（3個月超額 +5.25%, t=5.49）。單調性 ρ=+0.95。
  ⚠️ **不要把舊離散長線分的「70 分門檻」搬過來**——不同量表，只是數字長得像。

兩條路徑都要接上（本專案反覆踩的坑）：
- `universe.scan_universe()` → 整批掃完後算百分位，並 `save_distribution()` 落地
- `analysis.compute_scores()` → `score_single()` 對照那份分布打分
單檔頁面**不可以**拿手邊幾檔股票自己算百分位，否則同一檔在不同頁分數會不同。

⚠️ 沒跑過全市場掃描時 `score_single()` 回傳 `stale=True`、分數 None，
這是刻意的：寧可明講也不要給一個假的排名。

### 📉 策略已由 7 個整併為 5 個（2026-09-09）
`bestproven` 與 `momentum` 前 20 名重疊 **74%**，實質同一個東西 → 合併為 **`trend`**。
`sleeper` 與 `balanced` 同屬低基期逆勢家族、且都在多頭失效 → 合併為 **`contrarian`**。
保留 `sectorhot`（與 trend 只重疊 8%）、`lowpe`（使用者要求參考，已標示實證為負）、
`limitup`。app.py 裡那份「策略表查不到就退回 if-elif」的後備路徑已刪除——
它是第二份會與策略表不一致的邏輯來源。

### 🏷️ 卡片上的數字要標清楚用途（2026-09-09）
使用者問過「這些分數各自代表啥、哪個能當選股指標」，表示光有標籤不夠。
`services/ui.py: score_legend()` 是三頁共用的說明表，直接寫在頁面上：
趨勢結構分 ✅選股用、綜合評分 🟡體質總覽、潛力分 ⛔多頭失效、
本益比 ⛔單獨用虧錢、融資 ⛔只當風險警示。**新增分數時要同步加進這張表。**

`horizon_cells()` 已移除「長」那一格——它與趨勢結構分講同一件事，
同卡片放兩個「長期」數字（趨勢分 98 vs 長線分 94）使用者無從判斷該信哪個。

### 🚫 畫面上不准寫死實證數字（2026-09-09，已犯三次）
同一個錯誤犯了三次：說明文字寫死回測數字 → 換了模型 → 文字沒跟著改 →
畫面上出現「名字是新的、數字是舊的」，而且沒有任何測試會抓到。
實際發生過的：
1. 持股頁「長線結構分 +3.10% vs 綜合評分 +0.81%（179期）」——換模型後全錯
2. 選股頁「分數門檻實證：低於 70 分全為負…58.7%」——舊離散分的分桶表
3. 環境建議推薦「🏆 長線+量能確認」——那個策略已被整併刪除
4. `bar_note` 寫死「≥ 70」但 `TREND_BUY_BAR` 已改成 50

**規矩：任何出現在畫面上的回測數字，一律從實證檔讀出。**
- `services/evidence.py`：`trend_bucket_stats / trend_threshold / trend_monotonicity / trend_bucket_rows`
- `services/ui.py`：`threshold_note()`、`bucket_table()`、`evidence_badge()` 都已改成生成式
- 買進線唯一定義在 `services/scoring.BUY_BAR`，說明文字用 f-string 引用它

**另一條：查無資料 ≠ 表現不佳。** 分環境檢驗對新模型還沒做，
先前的程式把 `rs is None` 和 `rs["excess"] <= 0` 混在同一個分支，
於是「沒測過」被講成「這種盤勢下表現不佳」。已分開處理。

## 🏅 策略強弱只能看 `strategy_comparison.json`（2026-09-09）
`strategy_comparison.py` 把 5 個策略放進**同一批換股日、同一個基準**跑，
並直接呼叫 `strategies.select()`（測的就是實跑定義）。
**這是唯一可以拿來比較策略強弱的來源**——其他 run 的數字是不同批日期量的，
跨 run 比名次無效（本專案自己量過：全距 0.6~0.76% > 模型間差異）。

排名（持有3個月）：趨勢結構分 +7.14%(t=5.48) ＞ 漲停動能 +5.53% ＞ 強勢族群 +4.36%
＞ 超低本益比 −4.57% ＞ 逆勢潛伏 −4.65%。持有1個月則**漲停動能第一**(+2.86%, t=3.94)。

⚠️ **族群濾網被推翻**：舊結論「強勢族群+長線分是最佳(+3.07%)」是跨 run 比較的產物。
同場比較裡它輸給不加濾網的趨勢分。**這是第三次驗證「多加一層過濾更差」。**

UI 一律用 `ui.strategy_caption()` / `ui.strategy_table()` 產生說明，
**不要再寫「✅實證最強」這種形容詞**——使用者無從判斷誰比誰強。

### 🐛 全市場掃描要快取「全部」rows（2026-09-09）
先前 `st.session_state[cache_key] = enriched` 只存深度分析過的 ~40 檔，
而 shortlist 是用**掃描當下策略**的 `prelim_key` 挑的。於是快取宣稱的
「換策略只重新排序、不重抓」是假的：換策略時只能在別人挑剩的名單裡選。
漲停動能因此永遠 0 檔。現在存全部 566 檔、深度分析結果疊加，
並保留掃描時算的橫斷面 `trend_score`（比單檔對照分布更準）。

## 🔎 交叉篩選頁（2026-09-09）
`render_cross_screen_page()` —— 多選策略、找同時擠進各自前 N 名的股票。
**刻意沿用智能選股的 session 快取（`smart_full` / `smart_poplu` / `smart_pop`）**，
本頁不自己掃描；沒掃過就請使用者先去掃，而不是再跑一輪全市場。

⚠️ 交集**實測比單押更差**（第四次「多加一層過濾更差」）：
趨勢∩漲停 +6.49% vs 趨勢單獨 +7.14%；趨勢∩族群 +5.61%；漲停∩族群 +2.74% vs 漲停單獨 +5.53%。
頁面用 `ui.cross_evidence()` 把這張表**預設展開**顯示——功能給了，但不能讓人誤以為它更強。
數字來自 `strategy_comparison.json` 的 `cross_screen` 段。

## 🧹 稽核修正（2026-09-09）
使用者要求「找優化/整合/bug」時掃出來的，都已修：

1. **個股分析頁主視覺還是舊離散長線分** —— 台積電個股頁 94、持股頁 56.7。
   使用者為此問過兩次「到底幾分」。儀表板已改用 `a["trend"]["score"]`。
   沒跑過全市場掃描時顯示提示，不硬給分數。
2. **「四種週期評分」剩三種** —— `horizon_cards` 不再產生 `long`。
   長期只由趨勢結構分負責，全站一個長期數字。
3. **個股頁拿離散分去查趨勢分的分桶表** —— 不同量表，等於張冠李戴。已改。
4. **死碼**：`_select_by_strategy` 裡在 `raise KeyError` 後還留著一整段
   `if strategy == "momentum" / "sleeper" / ...` 的舊邏輯，永遠不可達。已刪。
5. **「排序用的週期評分」選單是死 UI** —— 整併後 5 個策略 `uses_horizon` 全為 False，
   選單永遠停用，還掛著舊 run 的說明數字。已移除。
6. **族群輪動頁宣稱「+3.07% 優於預設策略」** —— 那是**跨 run 比較**。
   同一次回測裡族群濾網 +4.36% 輸給不加濾網的 +7.14%。已改成從
   `strategy_comparison.json` 生成，並說明先前結論為何無效。
7. `evidence.py` 殘留已刪策略鍵、`universe.py` 仍算沒人用的 `prelim_balanced`、
   `limitup` 初篩鍵用不相干的 `total_score` —— 都已清理。

## 🧹 第二輪稽核修正（2026-09-09）
上一輪修完後再掃一遍，又抓到 **一整類同樣的錯：同一件事在畫面上有兩個數字**。
都已修，並且**把能自動抓的那幾類寫進 `check_consistency.py`**（見下節檢查 2）。

### 🔴 買進線曾經同時有 40 / 50 / 58 三個值
- `ui.long_threshold()` 回傳的是分桶表的**轉正點 40**，而不是買進線 50。
  個股頁儀表板與持股頁「實證門檻檢視」用它 → 一檔 45 分的股票在個股頁顯示
  「✅ 已達實證門檻」，在選股頁卻根本不會出現（選股按 50 篩）。
  `services/scoring.py` 本來就寫明**刻意不用轉正點**（40–50 那格 t=0.2~1.2，
  與 0 無異）。已改名為 **`ui.buy_bar()`** 並直接回傳 `scoring.BUY_BAR`。
- 選股頁長條圖畫「買進線 58／強力買進線 68／觀望線 48」，但 y 軸是**趨勢結構分**。
  已改成畫 `TREND_BUY_BAR` + 由 `trend_bucket_rows()` 生成的「優勢集中線」。
- 持股頁配置圖同樣寫死 58／48；摘要條標籤寫 `≥{buy_bar}` 但**計數用寫死的 58**
  （多頭 adj=−2 時標籤說 56、實際數 58）。
- 綜合評分的門檻 68/58/48/38 現在只定義在
  **`services/recommendation.BASE_THRESHOLDS`**，用 `buy_threshold(adj)` 取。
  **兩個量表不要混用**：趨勢分用 `scoring.BUY_BAR`、綜合評分用 `recommendation`。

### 🔴 `horizon["long"]` 已經不存在了，但還有三處在讀
`compute_scores()` 的 `horizon_cards` 移除「長」之後，**深度分析過的 row 就沒有
`horizon["long"]`**（掃描 row 還有）。於是三處 `.get("long", {}).get("score", 0)`
一律拿到 **0**，而且拿去比**趨勢分量表**的門檻：
1. 持股頁「替換候選」→ 最該出現的那幾檔（深度分析過的）永遠被排除
2. 族群頁「族群中最強個股」→ 深度分析過的全部墊底，且與 `sectorhot` 策略
   自己用的 `_trend()` 不是同一個指標，同一頁兩份名單互相矛盾
3. `sector.analyse_sectors()` 的 `median_long` → 中位數只算了沒被深度分析的那些
全部改用 `trend_score`（欄位改名 `median_trend`，表頭改「趨勢分(中位)」）。

### 🔴 持股頁的實證徽章餵錯量表
`ui.evidence_badge()` 查的是趨勢分分桶表，持股頁卻餵 `horizon_tech["long"]`（例 94）
→ 同一張卡片左邊「趨勢結構分 57」、右邊「趨勢分 94 落在 90-100 區間」。
個股頁上一輪已修，持股頁漏了——因為 `check_consistency.py` 的頁面比對
**只渲染個股分析與智能選股兩頁**，docstring 卻寫「三頁」。已把持股頁加進去。

### 🔴 全市場掃描後，單檔頁面仍說「尚未掃描」
`scoring.save_distribution()` 只寫檔案，**沒有更新 `_dist_cache`**。
若使用者先開個股分析（把 `{}` 快取起來）再去掃描，回來仍是 stale=True，
要重啟服務才會好。已在存檔時同步更新記憶體快取。

### 🔴 事後驗證表少了「長期」那一列
`render_backtest_verification()` 吃的是 `horizon_cards`，而 horizon_cards 移除
「長」之後 `rec_by_key.get("long")` 永遠是 None → **那一列被靜靜跳過**，
少的偏偏是唯一有實證背書的週期。已由趨勢結構分補上，判定門檻同時改成
趨勢分的買進線（`_verdict()` 原本寫死 58/48，那是綜合評分的量表）。

### 🟡 其他
- `scoring._pct_of()` 差一位：`searchsorted` 回的是插入位置（1~101），
  導致單檔分數系統性比掃描路徑高約 1 分，還會出現「贏過全市場 101%」。
- `ui.score_legend()` 的綜合評分權重寫「技術40%＋基本面30%」，
  **實際是 35/35/15/15**。已改成從 `recommendation.weight_note_str()` 生成。
- 個股頁「買進門檻 {58:+d}」印出來是「買進門檻 **+58** → 60」。
- `contrarian` 的 `bar_note` 寫「…**或**體質不弱且風報比 ≥1.5」，
  但那條 filter 在整併時就刪了。**`bar_note` 已改為由 filters 自動生成**
  （`strategies.bar_note(sdef)`），說明與實際條件不可能再不一致；
  額外補充放選填的 `note` 欄位。
- 選股頁開頭還寫「三種策略：強勢順勢／潛力潛伏／攻守兼備」——三個名稱都不存在了。
- 側邊欄「分析期間」在選股／交叉／持股三頁完全無作用，已改成只在個股分析頁顯示。
- 死碼：`strategies` 的 `_hscore/_hscore_key/_long_key/_combined/_hz_tech` 與
  5 個沒人用的 filter、`evidence` 的 `STRATEGY_TO_MODEL/MODEL_TO_STRATEGY/
  model_for/get_stats/regime_stats/best_strategies_for_regime/score_bucket_stats/
  buy_threshold/load_thresholds`（鍵名都是已刪策略；且後三個讀的是**舊離散分**的
  `score_thresholds.json`）、`sector.find_laggards`（補漲，實測 −0.25% 已否定）、
  卡片裡算完就被覆蓋的第一份 `rr_html`、`STRATEGIES` 的 `uses_horizon` 欄位、
  app.py 的 23 個沒用到的匯入（一整串計分函式，看起來像 app.py 還自己算分）。
- `recommendation._action_for()` 也自己寫了一次 68/58/48/38，已改讀 BASE_THRESHOLDS。
- `check_consistency.py` 的 ALLOW 是**子字串**比對，裡面放了 `"0.3"`（證交稅），
  於是「超額 **+0.35%**」這種真正該抓的數字也被放行。已改成正規化後精確比對。

## 🧹 第三輪稽核修正（2026-09-09）

### 🔴 選股頁的「回測實證」面板顯示的是**別的模型**的成績
面板原本用 `evidence_model` 去 `backtest_results.json` 查名字相近的舊模型，
5 個策略有 4 個對不上自己的定義：
| 策略 | 面板顯示的模型 | 為什麼不對 |
|---|---|---|
| `sectorhot` | 強勢族群+**長線分** | 排序早已改成趨勢結構分 |
| `contrarian` | 潛力潛伏 | 整併 sleeper+balanced 後篩選條件已不同 |
| `lowpe` | P1 純低本益比 | 沒有本策略的 3–12 倍區間限制 |
| `limitup` | （查不到→「尚未回測」） | 其實有 139 期同場比較資料 |

**改為一律讀 `strategy_comparison.json`** —— 那份是直接呼叫 `strategies.select()`
量出來的，測的就是 App 實跑的定義。修正後五個策略的數字（持有1個月）：
趨勢 +1.62%(t=2.20)／漲停 +2.86%(t=3.94)／族群 +1.24%(t=1.87，**不顯著**)／
低本益比 −1.88%／逆勢 −1.74%。並加上走查前後半段，前後變號會直接示警。
分環境那塊保留但**明確標示「量的是舊模型 X，不是本策略定義」**。
（順帶：`elif strategy == "lowpe"` 那段「我無法給你實證數據」是不可達的死碼——
lowpe 一直都查得到 `ev`，那段永遠不會顯示。已刪。）

### 🔴 流動性滑桿是死 UI
`scan_universe()` 在**評分迴圈裡**就用 `min_turnover` 濾掉股票，但 session 快取鍵
刻意不含門檻 → 掃完之後拖滑桿**完全沒有反應**，畫面卻寫著「調整門檻不會重抓，
只重新排序」。連帶第二個問題：**趨勢分的百分位分母會跟著滑桿變**——
分數號稱「贏過全市場 X%」，其實只贏過「流動性達標的那幾百檔」（0.5億時只有 398/1095）。
現改為一律評分到 `SCAN_FLOOR_TURNOVER`，門檻在 app.py 做記憶體過濾
（實測每檔評分僅 **3ms**，多評幾百檔只多 1~2 秒，遠比重抓便宜）。
交叉篩選頁也套用同一個門檻，否則兩頁都說「沿用同一批結果」卻不是同一批。

### 🔴 「建議部位上限」永遠顯示 0%
`calculate_risk_plan()` 的 `suggested_position_pct` 寫成
`2.0 / (risk / close * 100)`，分母已是百分比又再除一次 → **小 100 倍**。
停損 6% 的股票算出 0.33%，`{pos:.0f}%` 印成「建議部位上限約 **0%**」（正解 33%）。
承受度改用 `technical.RISK_PER_TRADE_PCT` 常數，畫面文字引用它。

### 🔴 漲停偵測完全看不到上櫃股
`limit_up.py` 用 `sid in OTC_STOCKS` 判斷上櫃——那是**只有 4 筆的離線 fallback 表**
（官方判斷在 `stock_data._is_otc()`）。而且今日漲停只抓證交所，櫃買完全沒抓。
於是「漲停動能」看不到任何上櫃股，**而連日漲停最常發生在上櫃小型股**。
已新增 `_fetch_tpex_today()`（沿用 universe 的櫃買快照，已快取），
`_compute_streaks` 與交易所標籤都改用 `_is_otc()`。
實測當日：上市 12 檔 + **上櫃 11 檔**，最強的是上櫃 5314 世紀* **連 10 日漲停**
（修正前完全掃不到）。

### 🔴 事後驗證表少了「長期」那一列
`render_backtest_verification()` 吃 `horizon_cards`，而 horizon_cards 移除「長」之後
`rec_by_key.get("long")` 永遠是 None → 那一列被靜靜跳過，少的偏偏是唯一有實證背書的
週期。已由趨勢結構分補上；`_verdict()` 原本寫死 58/48（綜合評分量表），
現在可指定門檻，趨勢分那列用趨勢分的買進線。

### 🟡 融資「去槓桿」的加分從來沒加上去
`margin.calculate_margin_signal()` 的 `penalty` 是**帶正負號**的（負值＝去槓桿），
理由字串也寫著「散戶去槓桿、籌碼趨安定 **(+6)**」——但 `generate_recommendation()`
只在 `> 0` 時套用，所以那 6 分一分都沒加；而
`generate_timeframe_recommendations()` 卻是不分正負都套用。
**同一個訊號在兩條計分路徑上行為不一致**。兩邊都改成套用帶號值。

### 🟡 其他
- `recommendation._action_for()` 又自己寫了一次 68/58/48/38 → 改讀 `BASE_THRESHOLDS`。
- `check_consistency.py` 檢查 3 的順序錯了：先算 core 再掃描，
  但掃描會**重寫**趨勢分的分布基準，於是比出來的差異是測試自己造成的
  （55 vs 66）。這個問題先前被 `save_distribution` 沒清快取的 bug 掩蓋住
  （新分布根本不生效，兩邊剛好都用舊的）。已改成**先掃描再算單檔**。

## 🏛️ 排除上櫃（2026-09-10）
智能選股頁新增「排除上櫃股（只看上市）」勾選。

**做法與流動性門檻相同：記憶體過濾，不改掃描範圍。**
掃描照樣算上市＋上櫃，只是不顯示上櫃股。原因是趨勢結構分是
「當日全市場橫斷面百分位」——若把上櫃整批抽掉再算百分位，同一檔股票在選股頁
與個股／持股頁就會出現兩個分數（那兩頁對照的是掃描落地的全市場分布）。
已實測：開關切換前後，上市股顯示的分數完全相同。

`scan_universe(include_otc=)` 參數保留，但 UI 不使用它——**不要**改成用它來實作
這個開關，那會讓分數跟著開關變。交叉篩選頁沿用同一組條件
（`smart_*_liq` / `smart_*_exotc`），否則兩頁都宣稱「沿用同一批結果」卻不是同一批。

⚠️ 順手修掉一個會讓這個篩選漏股票的既有 bug：`_analyze_one_stock()` 的
`exchange` 在沒有 limit_up_info 時**預設 "TWSE"**，而合併時 `{**base, **full}`
會用它蓋掉掃描算對的 "TPEX" → **每一檔被深度分析的上櫃股都被標成上市**
（卡片上的交易所徽章也一直是錯的）。已改為查 `stock_data._is_otc()`，
合併時也保住 base 的 `exchange` / `turnover`。

## ✅ 改完一定要跑 `check_consistency.py`（2026-09-09）
`AppTest` 只驗「頁面不拋例外」——頁面顯示**錯的數字**時它一樣是綠燈，
所以這個專案最常犯的兩類錯它一個都抓不到。`check_consistency.py` 補上：

1. **UI 字串裡不得寫死回測數字**（AST 掃 app.py / services/ui.py 的字串常數，
   找 `+N.NN%`、`t=N.NN`、`NNN期`、`勝率N%`）。第一次跑就抓到 9 處漏網。
2. **同一檔股票在各頁必須顯示同一個趨勢結構分**（真的把頁面渲染出來、
   從畫面文字挖數字比對，而不是比對內部變數——使用者看到的是畫面）。

**現在有三項檢查**（第 2 項是這輪新增的）：
1. UI 字串不得寫死回測數字
2. **UI 字串不得寫死門檻數字** —— 買進線／觀望線／買進門檻旁邊出現字面數字就算失敗
   （連 `f"買進門檻 {58:+d}"` 這種把數字塞進插值的寫法也抓）。
   一律引用 `scoring.BUY_BAR`（趨勢分）或 `recommendation.buy_threshold()`（綜合評分）。
3. 同一檔股票在**三頁**顯示同一個趨勢結構分（我的持股已補上，先前只驗兩頁）

```bash
python3 check_consistency.py            # 三項都跑（會跑一次全市場掃描，數分鐘）
python3 check_consistency.py --numbers  # 只跑靜態掃描（快，1 秒）
```
它已經抓到一次真的回歸：改 `score_legend` 時用字串切片，
連帶把 `ui.bucket_table` 整個刪掉，`app.py` 因此 ImportError。

⚠️ 白名單（ALLOW）比對的是**命中的那一段**，不是整個字串。
先前寫成整段跳過，結果一段文字只要提到 0.585% 手續費，
同段裡所有寫死的回測數字都被放行。

## 📈 持股頁的趨勢分變化（2026-09-09）
`services/trend_history.py` 每次持股頁重新評分就存一筆 `{日期: {股號: 分數}}`，
卡片上顯示 ▲/▼ 對比**最近一個不同日期**的快照（不是「上一次評分」——
同一天按五次重新評分不該產生五個比較基準）。
沒有前次資料時**不顯示**，不要拿 0 當「沒變化」。
`trend_history.json` 是個人資料，已列入 .gitignore。

## 🔎 交叉篩選的「至少命中 K 個」
⚠️ 只選 2 個策略時 K 只能是 2，此時**不能畫滑桿**：
Streamlit 的 `st.slider` 要求 `min_value < max_value`，min==max 會拋錯讓整頁掛掉。
